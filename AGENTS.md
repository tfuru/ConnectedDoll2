## EasyEDA 設計標準ワークフロー (EasyEDA Design Protocol)

EasyEDA Pro APIスキルを使用して回路設計・基板設計を行う場合、エージェントは以下の標準手順（フェーズ分割）を厳密に遵守すること。また、各ステップ完了時に進捗を必ず保存し、状態を確認すること。

### 共通原則
- **状態チェック**: 何らかの操作を行う前に、必ず `eda.dmt_Project.getCurrentProjectInfo()` を実行し、現在開いているプロジェクトの有無と名前を確認すること。
- **キャッシュの利用**: 部品の検索結果（UUIDなど）や中間データは、ワークスペースのローカルキャッシュ（`board/circuit/parts_cache.json`,`board/circuit/scripts` 等）に保存し、再実行時に無駄なAPI呼び出しやライブラリ検索が発生しないようにすること。

### フェーズ 1: 回路設計 (Schematic Design)
1. **プロジェクト管理**:
   - 目的のプロジェクトが既に開いている場合は新規作成せず、そのプロジェクトをそのままアクティブにする。開いていない場合のみ新規作成を実行する。
2. **部品の検索とキャッシング**:
   - `README.md`等の仕様書に指定された LCSC Part No または型番で部品検索を行う。
   - **在庫状況の確認**: 指定された部品の在庫がない（または不足している）場合は、速やかに電気的仕様・パッケージが同一で、在庫が豊富にある代替部品（JLCPCB PCBAサービスでの基本部品「Basic Part」を優先）を選定すること。代替部品を選定した場合は、仕様書（`README.md`）の該当箇所も合わせて更新すること。
   - 検索・選定した部品の UUID は直ちにローカルのキャッシュファイルに保存し、以降のステップではキャッシュを優先的に読み込むこと。
3. **部品の配置とネット接続**:
   - キャッシュした部品UUIDから回路図上に部品を配置する。
   - **部品の重なり防止**: 回路図上のシンボルやピンが他の部品と重なって視認性や接続操作に支障が出ないよう、十分な間隔を空けて配置座標を決定すること。
   - **機能ブロックごとの段階的な設計**: 回路の規模や複雑さに応じて、配置と配線は機能ブロック（例: 電源部、信号処理部、出力駆動部など）ごとに複数回に分けて段階的に実行することを推奨する。その際、各ステップの開始前にすでに配置済みの部品を走査し、二重配置を行わないように制御すること。
   - 仕様書で指定されたネットトポロジー（接続関係）に従い、正確に配線（Wire）を行う。電源やGNDなどのグローバルネットには、対応するネットポート（VCC/GND等）を割り当てる。
   - **ネットラベルの活用**: 長い配線や複雑な接続を行う際は、直接ワイヤを引き回して部品や他の配線と重なるのを防ぐため、ネットラベル（Netlabel）を効果的に使用して回路図の視認性を維持すること。




### フェーズ 2: 基板設計 (PCB Layout)
1. **同期（Update PCB）**:
   - 回路図のデータが確定した後、PCBドキュメントを作成し、同期（Update PCB）処理を実行する。
2. **物理制限の設定**:
   - 指定された基板外形（Board Outline）を作成する（原点 `(0, 0)` を基準とする）。
   - 取付穴（Mounting Hole）や切り欠きなどの物理的な制限を配置する。
   - 基板外形は、角丸を直線セグメントで近似する方式として閉じたポリラインとする
   - **基板外形とベタの座標位置**:
     - EasyEDA Proの座標系に基づき、原点 `(0, 0)` から設定された基板寸法（例：`60mm × 60mm` の場合は `X: 0 〜 2362.2 mil (60mm)`, `Y: 0 〜 2362.2 mil (60mm)`）の正しい正の領域に、完全に重ねて配置すること。
     - **座標指定の注意点（Y軸反転・ズレ防止）**: APIやスクリプトから矩形生成コマンド（例：`['R', x, y, width, height, ...]`）を使用して基板外形やベタを生成する場合、基準点 `(x, y)` は「左上（Left-Top）」座標となる。上方向がY軸の正となる EasyEDA Pro の座標系では、左上基準点のY座標を `0` に設定すると負の領域（下方向）に描画されてしまうため、必ず基板寸法の上端（例：高さ `H` の場合は `y = H`）を指定して正しい正の領域（`Y: 0 〜 H`）に描画されるよう制御すること。
3. **部品配置 (Placement)**:
   - 部品のフットプリントを仕様書で指定された基準座標、あるいは論理的に適切な位置（コネクタは基板端、パスコンはICピン近傍など）に配置する。
4. **配線ルールの適用と配線 (Routing)**:
   - 指定されたデザインルール（線幅、クリアランス）を設定する。
   - 電源、GND、各種信号線で推奨される太さを守り、片面または両面（Bottom層優先など）で配線を行う。
     - **信号線 (Signal Traces)**: 標準 `0.2 mm` (最小 `0.15 mm`)
     - **電源線 (Power Traces: VCC, 3.3V, 5V, V_BATT 等)**: 標準 `0.6 mm` 以上 (最小 `0.5 mm`。1A の通電容量を担保するため、主要電源経路は `0.6 mm` を厳守すること。目安として 1A あたり 0.6mm とする)
     - **グランド線 (GND Traces)**: 基本は両面GNDベタ接続（Copper Pour）とするが、個別に配線する場合は `0.8 mm` 以上を推奨とする。
       - **描画設定**: ベタGNDは「実心ベタ（Solid）」設定とすること。
       - **孤立銅箔設定**: 配線設計の途中段階や確認時は、一時的に「孤立銅箔を保留（Preserve Silos / Keep Island）」を有効にしてベタの形状を確認し、最終的なDRCおよび製造用データ出力時には無効化（削除）して不要な浮き銅箔を除去すること。
5. **シルクスクリーンの装飾**:
   - 部品参照名（RefDes）がフットプリントや他のテキストと重ならないように位置を調整する。
   - 電源の極性（`+`/`-`）や、LED/ダイオードの極性マークを視認性の良い場所に配置する。
6. **DRC（デザインルールチェック）の実行**:
   - 配線完了後、必ず `eda.pcb_Drc.check()` を呼び出し、エラーがないことを検証して報告する。

